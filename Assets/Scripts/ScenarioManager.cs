using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using static FrostweepGames.Plugins.GoogleCloud.SpeechRecognition.Examples.GCSR_DoCommandsExample;

public class ScenarioManager : MonoBehaviour
{
    public List<string> CountryList = new List<string>();
    public List<string> TicketList = new List<string>();
    public List<string> WelcomeList = new List<string>();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
